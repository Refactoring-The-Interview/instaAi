export enum AnimationSpinners {
    BORDER = "border",
    GROW = "grow",
}

export enum SpinnerColors {
    PRIMARY = "primary",
    SECONDARY = "secondary",
}

export enum SpinnerSize {
    SMALL = "sm",
    NORMAL = "undefined",
}
